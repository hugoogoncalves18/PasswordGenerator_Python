# 🔐 Password Generator in Python

This repository contains two simple Python scripts that generate secure passwords using random combinations of characters. The project is beginner-friendly and demonstrates basic concepts such as user input, loops, condition handling, and the `random` and `string` modules.

## 🚀 Features

### 1️⃣ Strong Password Generator
- Requires a **minimum of 12 characters**
- Automatically includes:
  - Lowercase letters
  - Uppercase letters
  - Digits
  - Special characters
- Uses percentage-based distribution for better password strength
- Shuffles characters to improve randomness

### 2️⃣ Custom Password Generator
- Lets the user choose:
  - Letters
  - Digits
  - Special characters
- User-defined password length
- Simple and flexible

## 🛠 Requirements

- Python 3.x  
- No external libraries required (only built-in modules)

## ▶️ How to Run

### Clone the repository
```bash
git clone https://github.com/your-username/password-generator.git
cd password-generator
python password_generator.py
python generator.py

